# maven 打包命令
mvn package

# 构建镜像命令
docker build -t jackkke/spring-boot-example-docker01:2.6.3 .

# dockerdesktop 启动镜像，开放端口设置为9716

# 浏览器访问http://localhost:9716, 返回 "hello! I'm from spring-boot-example-docker01"